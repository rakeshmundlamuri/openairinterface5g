# CPU build (this sandbox has no GPU). On a cloud GPU box, install PyTorch's CUDA build
# instead - e.g. for CUDA 12.4:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# then pip install the rest of this file (drop the two torch lines below).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
torchvision==0.29.0
numpy>=1.26
pillow>=10.2
pyarrow>=17.0
